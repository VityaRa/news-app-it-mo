import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  root: {
    borderColor: "gray",
    borderWidth: 1,
    margin: "auto",
    borderRadius: 8,
    marginTop: 12,
    marginBottom: 12,
    width: "100%",
  },
});
